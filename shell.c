#include "parse.h"

int main(int agrc, char** argv) {
    while(1) { //change condition
        //cmdLine = readline(printPrompt());

        //info = parse(cmdLine);

        //print_info(info);
    }

    //deallocate what's on the stack
    //free_info(info);
}
